#pragma once

using namespace std;

// Interface
class Strategy {
public:
	Strategy();
	//virtual void reinforce(Map, Player*) = 0;
	//virtual void attack(Player*) = 0;
	//virtual void fortify(Player*) = 0;
private:
};
